
async function Distributor(E) {

    const
        parent = document.getElementById('chessboard'),
        click = E.target;

    if (click.dataset.type === 'border' || click.dataset.team === 'null' || click === parent)
        return;


}









